import { FileText } from 'lucide-react';

export const user: { icon: React.ReactNode; text: string; href: string }[] = [
  // { icon: <History className="aspect-square size-6" />, text: `History`, href: `/history` },
  { icon: <FileText className="aspect-square size-6" />, text: `report`, href: `/report` }
];
